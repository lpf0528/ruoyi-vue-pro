<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.zc.dal.mysql.curtaintemplate.ZcCurtainTemplateMapper">

    <!-- 按款式ID查询模板行，LEFT JOIN 产品表取产品名称 -->
    <select id="selectByCurtainIdWithProductName"
            resultType="cn.iocoder.yudao.module.zc.dal.dataobject.curtaintemplate.ZcCurtainTemplateDO">
        SELECT
            t.id,
            t.curtain_id,
            t.structure_id,
            t.element_id,
            t.product_id,
            p.name AS product_name,
            p.one_price
        FROM zc_curtain_template t
            LEFT JOIN zc_product p ON t.product_id = p.id AND p.deleted = 0
        WHERE t.curtain_id = #{curtainId}
          AND t.deleted = 0
    </select>

</mapper>
