<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.iocoder.yudao.module.zc.dal.mysql.bills.ZcBillOrderItemsMapper">

    <select id="selectListWithOrderNoByBillId"
            resultType="cn.iocoder.yudao.module.zc.controller.admin.bills.vo.ZcBillOrderItemRespVO">
        SELECT
            t1.id, t1.bill_id, t1.order_id, t1.allocated_amount, t1.note,
            t2.order_no
        FROM zc_bill_order_items t1
        LEFT JOIN zc_sales_order t2 ON t1.order_id = t2.id AND t2.deleted = 0
        WHERE t1.deleted = 0
          AND t1.bill_id = #{billId}
        ORDER BY t1.id ASC
    </select>

</mapper>
